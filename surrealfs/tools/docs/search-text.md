Full-text search across the contents of every text file.

Matching is word-based and ignores case and common word endings, so "running"
also matches "run". Results are ranked by how often the terms appear, with a
short snippet of surrounding text.

Use this when you know roughly what a file says but not what it is called; use
`glob` when you know the name.
