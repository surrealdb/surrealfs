Write binary data (base64-encoded in `data_base64`) to a path.

Creates the file or replaces it entirely, creating missing parent folders. Set
`content_type` to the correct media type, e.g. `image/png`.
