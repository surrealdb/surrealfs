Delete a file from SurrealFS permanently.

Set `recursive` to true to delete a folder and everything inside it. A
non-empty folder will not be deleted without it.

This cannot be undone. Returns the number of items removed.
