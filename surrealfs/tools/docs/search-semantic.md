Search files by meaning rather than exact words.

Finds passages related to your query even when they share no vocabulary with
it — "how do I get paid" can match a file about invoicing. Use it when
`search_text` returns nothing useful, or when you are looking for a concept
rather than a phrase.

Only files that have been indexed for semantic search are considered.
