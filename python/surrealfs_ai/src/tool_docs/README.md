# Tool descriptions

Markdown blurbs for the pydantic-ai toolset in `examples/pydantic_ai_tools.py`.

- Filenames match tool names in kebab-case (e.g., `write-file.md`).
- Keep the first line as the title; follow with a short summary paragraph.
- Add optional sections for usage, options, and common errors when helpful.
