# cd
Change the current working directory for subsequent operations.

Usage: target may be absolute or relative; `..` cannot escape the root.
Errors: fails when the target path does not exist or is not a directory.
