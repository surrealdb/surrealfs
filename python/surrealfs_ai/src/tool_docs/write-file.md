# write-file
Write text content to the given path, creating or replacing the file.

Usage: provide `path` absolute or relative; `content` is written exactly as supplied. Parent directories must exist.
Notes: overwrites existing files; use `touch` to create empty files without content changes.
