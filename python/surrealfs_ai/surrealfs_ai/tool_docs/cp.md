# cp
Copy a file from `src` to `dest`, replacing any existing destination file.

Usage: paths may be absolute or relative to the current working directory.
Notes: copies file contents only; directories are not supported.
